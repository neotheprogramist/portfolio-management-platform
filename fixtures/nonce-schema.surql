DEFINE TABLE nonce SCHEMAFULL PERMISSIONS NONE;

DEFINE FIELD exp ON nonce TYPE datetime DEFAULT time::now() + 5m PERMISSIONS FULL;
DEFINE FIELD iat ON nonce TYPE datetime DEFAULT time::now() PERMISSIONS FULL;
DEFINE FIELD val ON nonce TYPE string PERMISSIONS FULL;
